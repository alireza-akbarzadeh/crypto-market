import { createSlice } from "@reduxjs/toolkit";

export const appPropertiesSlice = createSlice({
  name: "appProperties",
  initialState: {},
  reducers: {},
});

export const {} = appPropertiesSlice.actions;

export default appPropertiesSlice.reducer;
